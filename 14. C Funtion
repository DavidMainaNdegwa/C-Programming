Meant to divide our program into smaller chunks of code

SYNTAX
returnType functionName() {
...
...
}

//If a function does not return any value,use void as the return type

FUNCTION PARAMETERS

Example:

#include <stdio.h>
void calculateSquare (int number1,int number2){
int sum = number1 + number2;
printf("Square of %d and %d is %d\n",number1,number2,sum);
}

int main() {
int result = addNumbers(8,9);

return 0;
}

OUTPUT
The sum of 8 and 9 is 17

RETURN TYPES

Example:

#include <stdio.h>
int addNumbers(int number1,int number2) {
int sum  = number1 + number2;
return sum;
}

int main() {
int result = addNumbers(8,9);
printf("Result = %d",result);

return 0;
}

OUTPUT
Result = 17

FUNCTION PROTOTYPE
-This is a declaration of a function
-It provides information about the function name,parameters & return types

Example:

#include <stdio.h>

int addNumbers(int number1,int number2);

int main() {
int result = addNumbers(8,9);
printf("Result = %d",result);

return 0;
}
int addNUmbers(int number1,int number2) {
int sum = number1 + number2;
return sum;
}

OUTPUT
Result = 17

STANDARD LIBRARY FUNCTION
Characteristics
//print function - allows us to print the function
//sqrt()
//#include <math.h>
//float(f)

Example:

#include <stdio.h>
#include <math.h>

int main() {
float result = sqrt(25);
printf("Square root is %f",result);

return 0;
}

OUTPUT
Square root is 5.000000
